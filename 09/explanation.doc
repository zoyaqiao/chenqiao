

demo2
堆区存在的意义？
c++中如果变量都能够用引用来表示，那么其生命周期是可以很长的，那还需要堆区吗？


1.局部变量可以取多个别名吗？

Res:
1.只要用到了引用，那么改变只为地址i所指向的变量
2.引用在一定的环境下可以替代堆区

一般什么情况使用堆区
1.内容很多（比如一个结构体或一个类），可能不需要太频繁的访问，但生命周期长，通常很多个方法中可能都要用到
2.因为堆区就是各种慢，申请内存慢，访问慢，修改慢，释放慢，整理慢（所以说GC垃圾回收），但优点也不言而喻，访问随机灵活，空间超大，在不超可用内存的情况下你要多大就给多大。